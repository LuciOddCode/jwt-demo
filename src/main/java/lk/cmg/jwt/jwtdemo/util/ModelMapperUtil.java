package lk.cmg.jwt.jwtdemo.util;

/**
 * Package : lk.me.loginBackend.util
 * Date :3/22/2024
 * Time : 11:18 AM
 * Project : centralized-cms-api
 */

//@Configuration
public class ModelMapperUtil {

/*
    @Bean
    public ModelMapper modelMapper() {
        ModelMapper modelMapper = new ModelMapper();

        modelMapper.addMappings(new PropertyMap<Slider, SliderDTO>() {
            @Override
            protected void configure() {
               skip(destination.getStatus());
            }
        });

        modelMapper.addMappings(new PropertyMap<SliderDTO, Slider>() {
            @Override
            protected void configure() {
                skip(destination.getStatus());
            }
        });

        return modelMapper;
    }
*/

}
